<?xml version="1.0" encoding="UTF-8"?>
<faces-config version="2.2"
    xmlns="http://xmlns.jcp.org/xml/ns/javaee"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
                        http://xmlns.jcp.org/xml/ns/javaee/web-facesconfig_2_2.xsd">
    
    <!-- Application Configuration -->
    <application>
        <locale-config>
            <default-locale>en</default-locale>
        </locale-config>
    </application>
    
    <!-- Navigation Rules -->
    <navigation-rule>
        <from-view-id>/pages/productSearch.xhtml</from-view-id>
        <navigation-case>
            <from-action>#{searchController.goDashboard}</from-action>
            <from-outcome>dashboard</from-outcome>
            <to-view-id>/pages/dashboard.xhtml</to-view-id>
            <redirect/>
        </navigation-case>
    </navigation-rule>
    
    <navigation-rule>
        <from-view-id>/pages/orderWizard.xhtml</from-view-id>
        <navigation-case>
            <from-action>#{orderController.submit}</from-action>
            <from-outcome>orderConfirmation</from-outcome>
            <to-view-id>/pages/orderConfirmation.xhtml</to-view-id>
            <redirect/>
        </navigation-case>
    </navigation-rule>
    
    <!-- Managed Beans (if using classic JSF managed beans) -->
    <!-- Note: With CDI, managed beans should be defined with @Named annotation -->
    
</faces-config>
